import configData from './config.json'
export function configRouter(key: string) {
    // console.log("!!!configData", configData.server)
    // const root = configData.server;
    //todo: do it after I clean up the drive

}